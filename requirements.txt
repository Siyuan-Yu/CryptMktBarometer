# CryptMktBarometer dependencies (step 1: Flask only)

flask>=3.0.0,<4.0.0
pyyaml>=6.0,<7.0
apscheduler>=3.10,<4.0
requests>=2.31,<3.0
feedparser>=6.0,<7.0
# 可选：pip install deep-translator  增强英文标题在线翻译（未安装时自动用词典回退）
